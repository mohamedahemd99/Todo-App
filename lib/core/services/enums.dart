enum ExperienceLevel {
  fresh,
  junior,
  midLevel,
  senior,
}

enum Status { all, inProgress, waiting, finished }

enum StatusTodo { inProgress, waiting, finished }

enum Priority { low, medium, high }

enum MenuOption { edit, delete }
